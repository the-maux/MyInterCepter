<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/clear_terminal"
        android:title="Clear Screen"
        app:showAsAction="always"/>
    <item
        android:id="@+id/add_terminal"
        android:title="Add terminal"
        app:showAsAction="always"/>

    <item
        android:id="@+id/close_terminal"
        android:title="Close Terminal"
        app:showAsAction="always"/>

    <item
        android:id="@+id/show_history"
        android:title="Show history"
        app:showAsAction="always"/>

    <item
        android:id="@+id/clear_history"
        android:title="Clear history"
        app:showAsAction="always"/>

</menu>